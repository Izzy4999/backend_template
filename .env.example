# Copy this file to .env and fill in the values
# The server will refuse to start if any required variable is missing

NODE_ENV=development
PORT=4000

# JWT tokens
JWT_SECRET=
REFRESH_SECRET=

# Email (used by utils/mailer.ts)
EMAIL_USER=
EMAIL_PASS=
EMAIL_FROM=
